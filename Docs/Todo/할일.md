# GamedesignTool 할 일

> 공수 눈금은 **의견**이다 — 실측하면 고친다.
> **툴 전체에 3일을 넘겨 쓰지 않는다.** 넘어가면 설계가 큰 것이니 멈추고 다시 본다.

## 1판 — 관문 CLI + 상태 탭 : **끝 (2026-09-07)**

| 순 | 부품 | 무엇을 하나 | 예상 | 실제 | 상태 |
| --- | --- | --- | --- | --- | --- |
| ① | **상태 파일 모양** | 단계 · 판 번호 · 되돌아간 기록 · 물음 배열(물음 / 상태 / 기준) | 30분 | 설계에 포함 | 끝 |
| ② | **단계 정의 기본값 + 덮어쓰기** | 9단계 기본값 + `기획설정.json` 이 있으면 적힌 칸만 덮는다 | 30분 | 설계에 포함 | 끝 |
| ③ | **`init` · `show` · `next` · `back` · `ask` · `check`** | 상태 파일을 읽고 쓴다. `back` 은 까닭을 같이 남긴다 | 1~2시간 | ①~⑤ 를 한 판에 만들어 따로 못 갈랐다 | 끝 |
| ④ | **`lint` + `done` + 훅 셋** | 검사 L1~L8. `SessionStart` · `PreToolUse` · `Stop` 에 건다 | 1~2시간 | 위와 같음 | 끝 |
| ⑤ | **`dash` 상태 탭** | 정적 HTML 한 장. 스테퍼 · 지표 타일 3개 · 물음 목록 · 기둥 상자 · 문서 체크 · 고리 상태도 | 1~2시간 | 위와 같음 | 끝 |

**실제 합계 : 설계 한 판 + 구현·시험·문서 한 판.** 눈금(반나절)보다 짧게 끝났다 —
설계 문서에서 자료형과 검사 목록을 먼저 못 박아 두어 구현 중에 정할 것이 거의 없었기 때문이다.
**분 단위 실측값은 안 잰다** — 공수 툴로 재지 않았으므로 어림값을 적지 않는다.

- 시험 : `go test ./...` 8패키지 (검사 자체 시험 12개 포함). `testdata/repo-ok` · `repo-bad` 로 돌린다.
- ④의 검사는 **셀 수 있는 것만** 본다. 판단이 섞이는 것은 넣지 않았다.
- 프로토타입 폴더는 ④의 검사에서 **뺐다.**

### 1판을 만들며 밟은 함정 둘

| 함정 | 어떻게 됐나 |
| --- | --- |
| Go 의 `flag` 은 **첫 인자에서 옵션 읽기를 멈춘다** | `stage check <id> pass --note "…"` 가 통째로 실패했다. 파싱 전에 옵션을 앞으로 모으게 고쳤다 |
| 훅이 준 **상대 경로**가 현재 폴더 기준으로 풀렸다 | 검사가 조용히 아무것도 안 막았다. 저장소 뿌리 기준으로 풀게 고쳤다. **조용히 안 도는 검사가 가장 위험하다** |

### 붙일 때 할 일 (붙은 저장소 쪽)

- `.claude/settings.json` 에 훅 셋 걸기 (`show --hook` · `lint --hook` · `done --hook`)
- `.gitignore` 에 `Docs/Todo/대시보드.html` 넣기
- `stage init` 한 번

## 2판 — 프로토타입 갤러리 탭

| 순 | 부품 | 무엇을 하나 | 공수(의견) |
| --- | --- | --- | --- |
| ⑥ | 갤러리 탭 | 열 4개 칸반(만드는 중 · 확인 대기 · 통과 · 불통). 카드에 물음 한 줄 + 기준 개수 | 2~3시간 |
| ⑦ | meta 읽기 | 프로토타입 폴더의 meta 덩이를 훑어 카드 재료로 만든다. **읽기만 한다** | 1~2시간 |

- 프로토타입을 만드는 툴과는 **코드로 묶지 않는다.** 형식이 바뀌면 문서로 맞춘다.
- 프로토타입이 2개 이상 쌓인 뒤에 만든다. 하나뿐이면 갤러리가 필요 없다.

## 나중 — 밸런스 탭

| 순 | 부품 | 무엇을 하나 | 공수(의견) |
| --- | --- | --- | --- |
| ⑧ | 수치 데이터 읽기 · 스키마 검사 | JSON 여러 개(종류별로 쪼갬). 커밋 전에 오타·빠진 칸을 잡는다 | 반나절 |
| ⑨ | 곡선 그래프 + 식 보여 주기 | 값을 다 적지 말고 「식 + 검산용 몇 점」. 식을 글자 그대로 보여 주고 옆에 곡선 | 반나절 |
| ⑩ | **읽기 전용 미리보기 슬라이더** | 브라우저 안에서만 값을 흔들어 곡선이 어떻게 움직이나 본다. 파일은 안 건드린다 | 2~3시간 |
| ⑪ | N일 자동 플레이 시뮬 | 표만 봐서는 안 보이는 「고이는 자원 · 막히는 구간」을 찾는다 | 반나절 |

- **실제 수치가 생기기 전에는 안 만든다.** 세로 조각 단계에서 수치표를 쓸 때가 시작점이다.
  그 전에는 만들 데이터가 없어 상상으로 만들게 되고, 상상으로 만든 툴은 다 틀린다.
- ⑩은 **꼭 넣는다.** 스프레드시트를 버리며 잃는 것의 대부분을 이 하나가 메운다.
- 웹에서 **고치기까지** 만들면 공수가 세 배로 뛴다(2.5일 → 6~8일). 「보기만」으로 시작한다.

### 게임 저장소에서 써 보고 찾은 버그 (2026-09-08)

| 버그 | 어떻게 됐나 |
| --- | --- |
| `stage ask` 가 한국어 물음에서 **엉뚱한 id** 를 만든다 | `MakeID` 가 물음을 소문자화하고 영숫자 밖을 `-` 로 바꾸는데, 한국어는 전부 지워져 물음 속 숫자만 남는다. 「…넣은 2판 고리」 → id `2`. 숫자마저 없으면 `q<판>-<n>` 로 떨어져서 두 번째 물음만 `q2-2` 가 됐다. 게임 저장소에서는 `기획상태.json` 을 손으로 `q2-1` 로 고쳤다. **고칠 것** : 물음에 영문 낱말이 없으면 처음부터 `q<판>-<n>` 로 만들거나, 아예 늘 `q<판>-<n>` 로 만든다 (게임 저장소는 전부 한국어라 후자가 맞다) |

## 못 정한 것

| 무엇 | 왜 아직 못 정하나 |
| --- | --- |
| ~~`Stop` 훅의 `systemMessage` 가 실제로 화면에 뜨나~~ | **닫음 (2026-09-21).** 뜬다. 사용자가 「Stop says 가 자꾸 뜨는데 저게 뭐냐」고 물은 09-20 피드백이 뜬다는 증거다. 이제 문제는 안 뜨는 것이 아니라 **너무 자주 뜨는 것**이었고, 세션당 한 번으로 줄였다 |
| 옵션 이름을 한국어로도 받을까 | 지금은 영어(`--crit` · `--why` · `--note`)만 받는다. 써 보고 불편하면 둘 다 받게 한다 |
| 「단계 넘기기는 사용자만」 문구 | 게임 저장소에서 써 보니 매번 사용자가 치는 게 번거롭다. 뜻은 「판단은 사용자」이니 「사용자가 대화로 시키면 AI 가 대신 친다」로 스킬·README 문구를 푸는 것을 검토 (2026-09-07) |
| 컨셉 줄 상한 60줄 | 「A4 반 쪽」을 줄 수로 옮긴 어림값이다. 실제 컨셉 문서를 한 번 쓴 뒤 고친다 |

**정한 것 셋** (2026-09-07, 설계 문서 9장) : 단계 9칸 기본값 · 상태 파일 칸 이름 · 검사 목록 L1~L8.

## 근거

- 기획 루프 대시보드 조사 : 게임 저장소 `Docs/Research/2026-09-06-기획루프대시보드조사.md` (6장)
- 수치 대시보드 조사 : 게임 저장소 `Docs/Research/2026-09-06-수치대시보드툴조사.md` (추천 구성 · 7장)
- 절차와 훅 표 : `Docs/Guide/기획절차.md` (2026-09-06 에 옮겨 왔다. 정본이다)

## 2026-09-18 코드 리뷰 후속

전체 툴 코드 리뷰 판에서 이 툴은 발견 10건 중 8건(M1~M5·M8~M10)을 고쳤다
(9 패키지 ok · `bin/stage.exe` 다시 구움 · 훅 복사본 3파일을 ProtoTool 새 판에 맞춰 동기화).
아래가 남은 것이다.

### 보류 등급 (동작을 안 바꾸는 정리)

| # | 자리 | 무엇 |
| --- | --- | --- |
| M6 | `internal/dash/dash.go:171` | `dash` 와 `mdscan` 의 제목 규칙이 달라 **같은 파일에 다른 답**이 나온다 |
| M7 | `cmd/stage/cmd_state.go:140` | 단계 번호·이름이 코드에 박혀 있다. 설정으로 바꿀 수 있는 값인데도 |

### 이번 판에서 새로 드러난 것

- **실제 Claude Code 가 CRLF 파일에 `Edit` 를 거는 훅 왕복 1판을 사용자가 확인해야 한다.**
  M2 는 시험으로만 확인했다. 훅이 「편집이 적용됐나」를 판정하는 자리라 실물 1판이 필요하다.
- **M5 의 시험은 KST 에서만 뜻이 있다.** 다른 시간대 기계에서는 통과해도 아무것도 증명하지 않는다.
- 저장소가 CRLF 라 `gofmt -l` 이 전 파일을 찍는다.

## 2026-09-21 사용 피드백 묶음 반영

게임 저장소에서 쓰며 쌓인 피드백 6장(`Docs/Todo/2026-09-1*·2*-*피드백.md`)을 한 판에 반영했다.

| # | 무엇 | 어떻게 했나 |
| --- | --- | --- |
| A | L2 문구가 무엇을 하라는지 안 알려 준다 | 「지금 `<기획문서폴더>` 에 `03-시스템-*.md` 가 하나도 없습니다. 어느 것이든 한 장만 있으면 풀립니다」를 붙였다. **있는 문서 목록은 안 찍는다** — 그 목록은 늘 비어 있다(하나라도 있으면 안 막으니까) |
| B | `stage back 8` 이 거절돼 상태 파일을 손으로 고쳤다 | **앞 단계면 어디든 허용**(`--why` 는 그대로 필수). 고리 뒤(7~9)로 가면 판 번호·판시작일은 그대로고 기록만 한 줄 (설계 문서 결정 6-2) |
| C | 통과했을 때 무엇을 검사했는지 안 보인다 | `stage lint --verbose`. `lint.RunTrace` 가 **검사하는 그 자리에서** 돈 것·건너뛴 까닭을 적는다 (까닭을 따로 셈하면 실제 조건과 어긋나 거짓 안심이 된다) |
| D | 고친 파일 목록을 손으로 모아야 한다 | `stage lint --changed` — 툴이 git 을 부른다. 설계 문서 4장 아래 표 |
| E | Stop 훅 알림이 대화 내내 되풀이된다 | **내용은 그대로 두고 되풀이만** 줄였다. 세션 + 내용 해시가 같으면 안 찍는다. 할 일마다 「무엇을 하면 사라지나」 한 줄 (설계 문서 411절 아래) |
| F | 훅을 안 타는 판은 lint 를 직접 쳐야 한다 · 날짜 이름 문서 자리 | README · 스킬에 한 줄씩 |
| G | 실행 파일이 옛 판인 채 조용히 돈다 | `build.ps1` 과 `--version` 의 커밋·빌드시각. 「서브모듈을 갱신한 뒤마다」를 README·스킬에 |

- 시험 : 10패키지 ok. `internal/gitchanged` 새 패키지(Parse 표 시험 9줄 + 실제 git 통합 2건, git 이 없으면 건너뜀).
- **쓰는 뿌리가 하나 늘었다** (사용자 캐시 폴더). 보안 스킬 1절의 「셋 이하」 안이다 — 지금 둘.

## 2026-09-21 코드 리뷰 수정 8건

피드백 묶음을 반영한 뒤 돌린 코드 리뷰에서 나온 8건을 한 판에 고쳤다.

| # | 자리 | 무엇을 고쳤나 |
| --- | --- | --- |
| 1 | `build.ps1` | 네이티브 git 의 `2>$null` 이 `$ErrorActionPreference='Stop'` 에서 예외로 터져 **`.git` 없는 자리에서 빌드가 죽었다.** `try/catch` + `$LASTEXITCODE` 로 바꿨다. 덤으로 커밋 안 한 소스가 있으면 커밋 뒤에 `-dirty` 를 붙인다 |
| 2 | `lint --since <ref>` | `--changed` 는 작업 트리만 봐서 **서브에이전트가 커밋해 버리면 0개 종료 0** 으로 떨어졌다. `<ref>...HEAD` 의 커밋된 변경도 보게 했고(둘 다 주면 합침), 0개일 때 끝 글에 `--since` 를 안내한다 |
| 3 | `cmd/stage/done_seen.go` | tmp 이름이 고정이라 두 세션의 Stop 훅이 부딪혔다. `os.CreateTemp` 로 |
| 4 | `state.RoundBase` | 고리 뒤(9→8)로 되돌아가면 판시작일이 그대로라 **판 끝 할 일 검사가 옛 기록을 「다 했음」으로** 읽었다. 되돌아간 시각이 판시작일보다 뒤면 그 시각을 기준으로 본다 |
| 5 | `lint --changed` 셈 | 뿌리 밖 경로를 「제외」에 섞어 세서 `--root` 를 잘못 준 것이 안 보였다. 따로 세어 눈에 띄게 찍는다 |
| 6 | `internal/lint` 자취 | 기획문서폴더 **밖** 파일에도 「00-컨셉.md 가 아님」이 떴다. 「기획문서폴더 아래가 아님」으로 가른다 |
| 7 | `stage back` 끝 글 | 「N판 그대로」를 값 비교가 아니라 **고리 뒤로 갔나**로 가른다 |
| 8 | `config` · `state` 읽기 | PowerShell 5.1 의 `Set-Content -Encoding utf8` 이 붙인 **BOM 때문에 「깨졌습니다」 종료 5** 로 죽었다. 읽은 직후 BOM 을 뗀다 |

- 시험 : 11패키지 ok (`go vet` · `go test -count=1 ./...`). 새 시험 11개.
- 손 확인 : `.git` 없는 사본에서 빌드 성공(옛 판은 `NativeCommandError` 로 죽는 것도 같이 봤다) ·
  커밋된 새 `.cs` 가 `--since HEAD~2` 에서 L2 로 막힘(종료 2) · 틀린 ref 종료 3 ·
  BOM 붙은 설정·상태 파일로 `show` 종료 0.

### 짚어 둔 것 (안 고침이 정답)

- **M4 「기억 1건」을 판마다 다시 묻는 것은 설계가 맞다** (사용자 확인 2026-09-18).
  리뷰어는 잔소리라고 봤지만 의도된 동작이라 **문구만** 고쳤다.
